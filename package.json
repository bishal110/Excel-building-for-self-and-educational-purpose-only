{
  "name": "ai-office",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AI_Office — offline-first core office suite (Sheets, Docs, Slides)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "~5.8.2",
    "vitest": "^2.1.9"
  }
}
